data:
  batch_id: 0
  n_studies: 120
  seed: 0
  families: [esters, silicones]

split:
  val_fraction: 0.15
  test_fraction: 0.15
  seed: 17

model:
  hidden: [64, 64]
  dropout: 0.0

train:
  epochs: 150
  batch_size: 256
  lr: 0.01
  weight_decay: 0.0001
  seed: 42
